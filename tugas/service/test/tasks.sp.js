/* eslint-disable no-undef */

describe('task', () => {
  describe('task', () => {
    it('get test', async () => {
      const test = 'test';
      expect(test).toBe('test');
    });
  });
});

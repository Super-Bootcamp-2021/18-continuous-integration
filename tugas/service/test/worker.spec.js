/* eslint-disable no-undef */

describe('worker', () => {
  describe('worker', () => {
    it('get test', async () => {
      const test = 'test';
      expect(test).toBe('test');
    });
  });
});
